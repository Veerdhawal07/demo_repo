#include <bits/stdc++.h>
#define ll long long
#define vll vector<long long>
#define vi vector<int>
using namespace std;
void solve()
{
    int n;
    cin >> n;

    for(int i = 0;i<n;i++);

    while(n > 0);
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
}